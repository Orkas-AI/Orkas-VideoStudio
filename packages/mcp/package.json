{
  "name": "@orkas/video-studio-mcp",
  "version": "0.0.0",
  "description": "MCP server for OrkasVideoStudio — mirrors the `ovs` CLI as typed tools.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "ovs-mcp": "./dist/index.js"
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@orkas/video-studio-core": "workspace:*",
    "@orkas/video-studio-tools": "workspace:*",
    "@orkas/video-studio-skills": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
